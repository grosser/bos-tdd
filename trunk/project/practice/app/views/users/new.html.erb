<h1>New user</h1>

<%= error_messages_for :user %>

<% form_for(@user) do |f| %>
  <%= render :partial => 'form', :locals =>{:f => f}%>
  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', users_path %>
